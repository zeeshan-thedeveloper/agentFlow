{
  "name": "agentflow",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "dev": "corepack pnpm --parallel -r dev",
    "build": "corepack pnpm -r build"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "pnpm": {
    "overrides": {
      "fast-uri": ">=3.1.2",
      "lodash": ">=4.18.0",
      "multer": ">=2.1.1"
    }
  }
}
